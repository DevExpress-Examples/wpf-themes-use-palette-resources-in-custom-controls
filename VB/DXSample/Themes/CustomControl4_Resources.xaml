<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DXSample"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
        xmlns:dxci="http://schemas.devexpress.com/winfx/2008/xaml/core/internal">

    <Style TargetType="{x:Type local:CustomControl4}">
        <Setter Property="BorderThickness" Value="3" />
        <Setter Property="BorderBrush" Value="{dxci:ThemeResource ThemeKey={dxt:PaletteBrushThemeKey  ResourceKey=Border}}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="Background" Value="{DynamicResource {dxt:PaletteBrushThemeKey  ResourceKey=Control.Background, ThemeName=VS2019Blue}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomControl4}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <TextBlock Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Text}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
