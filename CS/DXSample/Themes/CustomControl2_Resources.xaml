<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:DXSample" 
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys">
    <Style TargetType="{x:Type local:CustomControl2}">
        <Setter Property="BorderBrush" Value="{DynamicResource {dxt:PaletteBrushThemeKey ResourceKey=Backstage.Window.Background, ThemeName=Office2019Colorful}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomControl2}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="3">
                        <TextBlock Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Text}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
