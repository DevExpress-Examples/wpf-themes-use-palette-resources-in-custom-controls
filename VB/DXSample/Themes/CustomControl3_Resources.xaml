<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DXSample"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
        xmlns:dxci="http://schemas.devexpress.com/winfx/2008/xaml/core/internal">
    <Style TargetType="{x:Type local:CustomControl3}">
        <Setter Property="BorderThickness" Value="3" />
        <Setter Property="BorderBrush" Value="{dxci:ThemeResource ThemeKey={dxt:PaletteBrushThemeKey  ResourceKey=Border}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomControl3}">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <TextBlock Text="{TemplateBinding Text}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
